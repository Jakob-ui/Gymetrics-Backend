name: Deploy to Dockerhub

on:
    push:
        branches:
            - main

jobs:
    build-push:
        name: Build and Push Docker Image
        runs-on: ubuntu-latest
        steps:
            - name: Checkout source code
              uses: actions/checkout@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKER_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Build and push production image
              uses: docker/build-push-action@v6
              with:
                  context: backend
                  file: backend/Dockerfile
                  push: true
                  platforms: linux/amd64
                  tags: |
                      ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKERHUB_PROJECT_NAME }}:latest

    build-push-scrapeservice:
        name: Build and Push Docker Image for scrape service
        runs-on: ubuntu-latest
        steps:
            - name: Checkout source code
              uses: actions/checkout@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKER_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Build and push production image
              uses: docker/build-push-action@v6
              with:
                  context: scrapeService
                  file: scrapeService/Dockerfile
                  push: true
                  platforms: linux/amd64
                  tags: |
                      ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKERHUB_SCRAPESERVICE_PROJECT_NAME }}:latest